/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true, // Asegúrate de que las URLs terminen en barra
    output: 'export',    // Configura la salida para la exportación estática
  };
  
  export default nextConfig;
  